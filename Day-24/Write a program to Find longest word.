# Program to find the longest word in a sentence

sentence = "Python programming is amazing and powerful"

words = sentence.split()

longest_word = ""
max_length = 0

for word in words:
    if len(word) > max_length:
        max_length = len(word)
        longest_word = word

print("Longest word:", longest_word)
print("Length:", max_length)
