# Program to find column-wise sum of a matrix

A = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

rows = len(A)
cols = len(A[0])

for j in range(cols):
    col_sum = 0
    for i in range(rows):
        col_sum += A[i][j]
    print(f"Sum of column {j+1}: {col_sum}")
