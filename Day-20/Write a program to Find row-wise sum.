# Program to find row-wise sum of a matrix

A = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

rows = len(A)
cols = len(A[0])

for i in range(rows):
    row_sum = 0
    for j in range(cols):
        row_sum += A[i][j]
    print(f"Sum of row {i+1}: {row_sum}")
